# Ignore folders.
.storage
.cloud
.google.token
tts

# Ensure these YAML files are ignored, otherwise your secret data/credentials will leak.
ip_bans.yaml
secrets.yaml
known_devices.yaml
zones.yaml

home-assistant.log
image
*.db
*.db-shm
*.db-wal
*.pyc
